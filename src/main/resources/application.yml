spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/task_db
        username: task
        password: task_pw
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        database: postgresql